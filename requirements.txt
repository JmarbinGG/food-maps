argon2-cffi==25.1.0
boto3==1.34.162
bcrypt==4.1.2
# Required by PyMySQL to authenticate against MySQL 8's default
# caching_sha2_password plugin. Without it, connecting raises
# "'cryptography' package is required for sha256_password or
# caching_sha2_password auth methods" and the app cannot reach the database.
# Production has been surviving on an RDS user created with the legacy
# mysql_native_password plugin; any newly provisioned user or instance
# defaults to caching_sha2_password and fails without this.
cryptography==50.0.0
email-validator==2.3.0
fastapi==0.121.3
httpx==0.28.1
mysql-connector-python==9.4.0
passlib==1.7.4
psycopg2-binary==2.9.9
pydantic==2.10.6
pyjwt==2.12.0
pymysql==1.1.1
python-dotenv==1.0.0
python-multipart==0.0.26
sqlalchemy==2.0.23
twilio==8.10.0
uvicorn==0.24.0
# AI agent stack - upper bounds keep installs on 0.3-era APIs used by backend/agent/
openai>=1.10.0,<2
langgraph>=0.2.45,<0.3
langchain>=0.3.11,<0.4
langchain-openai>=0.2.10,<0.3
langchain-core>=0.3.27,<0.4
langchain-community>=0.3.10,<0.4
langgraph-checkpoint>=2.0.8,<3
